<!DOCTYPE html>
<html lang="en">

    <%- include('../partials/head'); %> 
    <%/* include() Esse é um comando do EJS */%>

    <body class="container">
        <%- include('../partials/header'); %>
        <main>
            <div class="jumbotron">
                <h1>Node + EJS</h1>
                <p>Let's build a project with EJS files.</p>
                <p><%= subtitle %></p>
                <!-- Para mostrar o conteúdo do EJS, precisa do sinal de "=" depois do porcentagem -->
            </div>
            <ul class="list-group">
                <% qualities.forEach(function (quality){ %>
                    <li class="list-group-item">
                        <strong><%= quality.title %></strong> <%= quality.message %>
                    </li>
                <% }) %>
                
            </ul>
        </main>

    <%- include('../partials/footer'); %>

    </body>
</html>